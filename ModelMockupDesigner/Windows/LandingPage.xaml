<UserControl x:Class="ModelMockupDesigner.LandingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModelMockupDesigner"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0">
            
        </Grid>

        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid x:Name="recentProjects" Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Recent Projects" FontSize="25" HorizontalAlignment="Center" />
                <Border Grid.Row="1" BorderBrush="Black" BorderThickness="2" Margin="10">
                    <Grid>
                        <!-- List of recent projects to be displayed here -->
                    </Grid>
                </Border>
            </Grid>
            <Grid x:Name="buttons" Grid.Column="1">
                <StackPanel>
                    <Button Content="New"  Width="100" Height="50" Margin="15" />
                    <Button Content="Load" Width="100" Height="50" Margin="15"/>
                    <Button Content="Export" Width="100" Height="50" Margin="15"/>
                    <Button Content="Exit" Width="100" Height="50" Margin="15" Click="ExitApplication_Click"/>
                </StackPanel>
            </Grid>

        </Grid>

    </Grid>
</UserControl>
